name: be-ci

on:
  pull_request:
    paths:
      - "backend/**"
      - ".github/workflows/be-ci.yml"
  push:
    branches: ["dev", "feat/**"]
    paths:
      - "backend/**"
      - ".github/workflows/be-ci.yml"

permissions:
  contents: read

concurrency:
  group: be-ci-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: backend

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    env:
      CI: true
      SPRING_PROFILES_ACTIVE: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # gradle wrapper 보안 검증
      - name: Validate Gradle Wrapper
        uses: gradle/wrapper-validation-action@v2

      # Gradle 실행 JVM(JDK 24)
      - name: Setup Java (Gradle JVM = 24)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "24"
          cache: gradle

      # 코드 toolchain용 JDK 25 미리 설치
      - name: Provision Java 25 for toolchain
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: wrapper
          cache-read-only: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork }}

      # .env 파일 생성
      - name: Create .env file
        run: |
          echo "${{ secrets.DOT_ENV_CI }}" > .env
          echo "✅ .env file created"

      # Docker Compose로 MariaDB & Redis 시작
      - name: Start MariaDB and Redis
        run: |
          docker compose up -d mariadb redis
          echo "⏳ Waiting for services to be ready..."
          sleep 10

      # 서비스 헬스체크
      - name: Wait for MariaDB
        run: |
          until docker exec chwimeet-mariadb mariadb-admin ping -h localhost -uroot -p${{ secrets.DB_PASSWORD }} --silent; do
            echo "Waiting for MariaDB..."
            sleep 2
          done
          echo "✅ MariaDB is ready"

      - name: Wait for Redis
        run: |
          until docker exec chwimeet-redis redis-cli -a ${{ secrets.REDIS_PASSWORD }} ping | grep -q PONG; do
            echo "Waiting for Redis..."
            sleep 2
          done
          echo "✅ Redis is ready"

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Print versions
        run: |
          ./gradlew --version
          java -version

      - name: Initialize database schema
        run: |
          echo "=== Loading schema from SQL file ==="
          
          # schema.sql이 있으면 직접 로드
          if [ -f "src/main/resources/schema.sql" ]; then
            docker exec -i chwimeet-mariadb mariadb -uroot -p${{ secrets.DB_PASSWORD }} chwimeet < src/main/resources/schema.sql
            echo "✅ Schema loaded from SQL file"
          else
            echo "❌ schema.sql not found"
            exit 1
          fi
          
          # 테이블 확인
          echo "=== Tables in database ==="
          docker exec chwimeet-mariadb mariadb -uroot -p${{ secrets.DB_PASSWORD }} -e "USE chwimeet; SHOW TABLES;"

      - name: Generate JOOQ sources
        env:
          SPRING__DATASOURCE__URL: jdbc:mariadb://localhost:3306/chwimeet
          SPRING__DATASOURCE__USERNAME: root
          SPRING__DATASOURCE__PASSWORD: ${{ secrets.DB_PASSWORD }}
        run: |
          echo "=== Starting JOOQ Generation ==="
          ./gradlew generateJooq --no-daemon
          
          if [ -d "build/generated-src/jooq/main/com/back/jooq/tables" ]; then
            echo "✅ JOOQ generated successfully"
            ls -la build/generated-src/jooq/main/com/back/jooq/tables/
          else
            echo "❌ JOOQ generation failed"
            exit 1
          fi

      # 테스트 + 커버리지
      - name: Run tests (JUnit5) with coverage
        env:
          SPRING__DATASOURCE__URL: jdbc:mariadb://localhost:3306/chwimeet
          SPRING__DATASOURCE__USERNAME: root
          SPRING__DATASOURCE__PASSWORD: ${{ secrets.DB_PASSWORD }}
        run: ./gradlew test jacocoTestReport -x generateJooq --info --stacktrace --no-daemon

      # 빌드(테스트/커버리지 재실행 방지)
      - name: Assemble (no tests)
        env:
          SPRING__DATASOURCE__URL: jdbc:mariadb://localhost:3306/chwimeet
          SPRING__DATASOURCE__USERNAME: root
          SPRING__DATASOURCE__PASSWORD: ${{ secrets.DB_PASSWORD }}
        run: ./gradlew assemble -x test -x jacocoTestReport --no-daemon

      # 테스트 결과 업로드
      - name: Upload test results (JUnit XML)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-results
          path: backend/build/test-results/test
          if-no-files-found: ignore
          retention-days: 3

      # 테스트 HTML 리포트
      - name: Upload test report (HTML)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-report-html
          path: backend/build/reports/tests/test
          if-no-files-found: ignore

      # 커버리지 리포트 업로드
      - name: Upload coverage report (JaCoCo HTML)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: backend/build/reports/jacoco/test/html
          if-no-files-found: ignore
          retention-days: 3

      # Docker Compose 정리
      - name: Stop Docker services
        if: always()
        run: docker compose down -v